let new_image = '' // place the url of the new image you want here

// add an onload to define a function that runs when the current tab has loaded
// in the function final all the images on the current page
// then loop through them and change all their src's
